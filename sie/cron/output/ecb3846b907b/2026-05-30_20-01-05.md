# Cron Job: hermes-web-computer: rebuild + deploy check (FAILED)

**Job ID:** ecb3846b907b
**Run Time:** 2026-05-30 20:01:05
**Schedule:** 0 16,20 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Rebuild and verify hermes-web-computer:

1. Verify node/npm: node --version && npm --version
2. cd /opt/data/hermes-web-computer
3. Check git status: git log --oneline -3
4. Build Go backend: cd /opt/data/hermes-web-computer/backend && go build ./...
5. Build frontend: cd /opt/data/hermes-web-computer && npm run build
6. Run Go tests: cd /opt/data/hermes-web-computer/backend && go test ./...
7. If all pass, report success. If failures, fix them, commit, push.
8. Check if the server is running on port 3005: curl -s -o /dev/null -w "%{http_code}" http://localhost:3005
9. If not running, start it: cd /opt/data/hermes-web-computer && nohup go run ./backend/cmd/server/main.go > /tmp/hwc-server.log 2>&1 &
10. Report status concisely.

## Error

```
RuntimeError: Connection error.
```
