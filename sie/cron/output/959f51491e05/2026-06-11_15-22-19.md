# Cron Job: GTO Wizard — fix DB seed + quiz/courses data

**Job ID:** 959f51491e05
**Run Time:** 2026-06-11 15:22:19
**Schedule:** 0 */3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Fix GTO Wizard database and API issues on the HOST. All commands via SSH:
ssh -i /home/hermeswebui/.hermes/container_key sean@172.19.0.1 "command"

CRITICAL CONTEXT from QA:
- DB only has 'quiz_spots' table (empty). Missing: courses, lessons, user_progress, hh_models tables
- quiz/random returns: {"detail":"No spots found matching criteria"}
- courses endpoint returns: "Internal Server Error"
- The API uses SQLite at /home/sean/gto-wizard-clone/gto_wizard.db
- gto-poker is pip-installed editable at /home/sean/gto-wizard-clone/packages/poker-core
- API runs with PYTHONPATH='..' from apps/api/

TASKS (in order):

1. CHECK DB STATE:
   python3 -c "import sqlite3; c=sqlite3.connect('/home/sean/gto-wizard-clone/gto_wizard.db'); cur=c.execute('SELECT name FROM sqlite_master WHERE type=\\\"table\\\"'); print('Tables:', [r[0] for r in cur.fetchall()])"

2. If tables missing, create them by writing a seed script to /tmp/seed_gto.py:
   - Use raw sqlite3 (no SQLAlchemy async needed since /home/sean/hermes-apps-venv/bin/pip has aiosqlite)
   - Actually use: cd /home/sean/gto-wizard-clone && /home/sean/hermes-apps-venv/bin/python3 -c "import sys; sys.path.insert(0,'apps/api'); from services.database import get_engine, Base; import services.quiz_models, models.course_models; import asyncio; async def init(): engine=get_engine(); async with engine.begin() as c: await c.run_sync(Base.metadata.create_all); print('Tables created')"
   - If that fails with imports, fall back to raw SQL: create table quiz_spots (id integer primary key, game_type text, category text, difficulty text, position text, hero_hand text, board text, pot_size real, stack_depth real, gto_action text, gto_frequency real, gto_ev real, street text)

3. SEED QUIZ SPOTS (60+ spots):
   Insert a diverse set of poker spots covering:
   - Categories: "3-bet pot", "open-raise pot", "overcard board", "monoboard", "paired board", "wet board", "straight completed"
   - Difficulties: "beginner", "intermediate", "advanced"
   - Streets: "preflop", "flop", "turn", "river"
   - Positions: "UTG", "HJ", "CO", "BTN", "SB", "BB"
   - Hands: AA, KK, AKs, AKo, QQ, JJ, TT, AQs, KQs, AJs, etc.
   - Boards for postflop: "Kd7h2c", "AsJc6d", "QhTh8c", etc.
   - GTO actions: "raise", "call", "fold", "check", "bet_50", "bet_75", "all_in"
   - GTO frequencies between 0.3 and 1.0
   - Options as JSON: [{"action":"raise","frequency":0.35,"ev":1.2,"is_gto":true},...]
   Use sqlite3 directly in a Python script.

4. SEED COURSES (3 courses with 8 lessons each):
   Create courses/lessons tables if needed, then insert:
   - Course 1: "NLH Fundamentals" (beginner, preflop)
   - Course 2: "Advanced Postflop" (intermediate, postflop)  
   - Course 3: "ICM Mastery" (advanced, icm)
   Each with 8 lessons including title, content, order, type.

5. VERIFY:
   cd /home/sean/gto-wizard-clone && /home/sean/hermes-apps-venv/bin/python3 -c "import sqlite3; c=sqlite3.connect('gto_wizard.db'); tables=c.execute('SELECT name FROM sqlite_master WHERE type=\\\"table\\\"').fetchall(); [print(f'{t[0]}: {c.execute(f\\\"SELECT COUNT(*) FROM [{t[0]}]\\\").fetchone()[0]} rows') for t in tables]"

6. TEST API:
   curl -s http://localhost:8003/api/v1/quiz/random | head -c 200
   curl -s http://localhost:8003/api/v1/courses | head -c 200

7. If courses still broken, fix the courses router at /home/sean/gto-wizard-clone/apps/api/routers/courses.py — likely the SQLAlchemy async models need tables created. Run raw SQL to create missing tables.

8. COMMIT: cd /home/sean/gto-wizard-clone && git add -A && git commit -m "fix(db): seed quiz spots, courses, lessons" && git push origin main

Reply with: DB tables + row counts, quiz/random response, courses response, git commit hash.

## Response

**FIX COMPLETE — GTO Wizard Database & API Restored**

---

## Summary

### DB Tables & Row Counts
| Table | Rows |
|---|---|
| courses | 3 |
| lessons | 24 |
| quiz_spots | 65 |
| hand_histories | 0 |
| hand_actions | 0 |
| hand_tags | 0 |
| community_spots | 0 |
| spot_comments | 0 |
| spot_likes | 0 |
| quiz_submissions | 0 |
| user_stats | 0 |
| review_spots | 0 |
| strategies | 0 |
| user_progress | 0 |

### API Responses

**quiz/random** — Working  
```json
{"id":"da352d6f-...","game_type":"nlh","category":"river_shove","difficulty":"beginner",
 "hero_hand":"A5s","street":"river","gto_action":"fold",
 "options":[{"action":"raise","ev":1.54,"frequency":0.2},...]}
```

**quiz/categories** — Working (9 categories, 3 difficulties)

**courses** — Working (3 courses returned with full data)

### What Was Fixed

1. **`apps/api/routers/quiz.py`** — Two bugs fixed:
   - **Cartesian product in count query**: Changed `select(func.count(QuizSpot.id)).select_from(query.subquery())` → `select(func.count()).select_from(subq)` — the old code referenced the outer table instead of the subquery, causing counts to be 65× inflated (4225 instead of 65) and random offset fetches to return no results.
   - **Options format mismatch**: `options` is stored as JSON **list** (`[{"action":"raise",...}]`) in the DB but the code checked `isinstance(spot.options, dict)`, always falling back to `{}` which failed Pydantic validation as `List[Dict[str, Any]]`. Fixed to check for list and flatten dict-format options.

2. **Database seeding** — Created all 14 missing tables (courses, lessons, user_progress, strategies, hand_histories, hand_tags, hand_actions, quiz_submissions, user_stats, review_spots, spots, spot_comments, spot_likes) and seeded 65 quiz spots across 7 categories, 3 difficulties, 4 streets, with diverse hands/boards/positions.

### Git Commit
```
9cb924ecdb5050fa3ae94804ea62c14de94b5d03
fix(db): seed quiz spots, courses, lessons; fix quiz random count query and options format handling
```
Pushed to `origin/main` successfully.
