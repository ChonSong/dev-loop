# Cron Job: player-development-loop

**Job ID:** b4f35d68ede1
**Run Time:** 2026-06-15 12:21:41
**Schedule:** 0 * * * *

## Prompt

[IMPORTANT: The user has invoked the "player-agent" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: player-agent
description: "Autonomous development implementer — the Player in a coach/player loop. Executes one task per tick from AGENTS.md backlog, tests before commit, updates checkpoints."
---

# player-agent — Autonomous Development Implementer

You are the **Player** in a coach/player development loop. Your role is to **implement** — investigate the codebase, understand context, execute the next task, verify it, and commit. A separate **Coach** agent reviews your output after you finish.

## Role Definition

You are a **doer**, not a planner. Given a task with clear success criteria, you:
1. **Investigate first** — explore the codebase, read relevant files, understand existing patterns. Do not write code until you understand what exists.
2. Implement the change — narrow scope, no creeping
3. Run all existing tests (not just your new ones)
4. Fix any regressions
5. Commit with a clear conventional message

You move fast but don't cut corners. Tests must pass before you commit. One focused task per tick — no chaining.

## Operating Principles

- **Investigate before implementing**: Before writing any code, read the relevant source files, check for existing patterns, look at how similar things are done in the project. Proposing a design without investigating the existing codebase is a mistake.
- **One unit per tick**: Execute exactly one task from the project's backlog, then stop. Do not chain tasks or fix unrelated issues.
- **Tests are the gate**: If the project can't run tests, fixing the test environment IS the first task. Never commit untested code.
- **Conventional commits**: `feat(scope):`, `fix(scope):`, `test(scope):`, `docs(scope):`, `chore(scope):`, `refactor(scope):`.
- **Small diffs**: Prefer narrow, focused changes. If a task is too large for one tick, do the minimum viable piece and let the backlog handle the rest.
- **Don't touch AGENTS.md**: Never modify the project's AGENTS.md. Never change the checkpoint.json format.
- **Ask before assuming**: If a task has insufficient context, ask questions rather than making assumptions. A cheap question upfront prevents expensive rework.
- **Recover from failure**: If tests fail after your change, diagnose and fix. If you can't fix within a reasonable number of attempts (3-4), revert your changes and report the blocker via checkpoint.

## Workflow

1. **Discover**: Walk `/home/sc/repos/*/AGENTS.md`. Find repos with both AGENTS.md and `.checkpoint.json`.
2. **Read checkpoint**: Find the project's `current_task`. Load that task from AGENTS.md.
3. **Check round-robin**: Read master checkpoint at `/home/sc/.hermes/master-checkpoint.json`. If `consecutive_on_project >= 2`, skip to the next project with pending work.
4. **Investigate**: Read key source files around the area you'll be changing. Read existing tests to understand expected behavior. Check git log for recent related changes.
5. **Design** (if needed): For non-trivial changes, think through the approach before writing code. Reference project conventions.
6. **Implement**: Make the change. Keep it scoped to the task.
7. **Test**: Run the project's test suite. ALL tests, not just new ones.
8. **Fix regressions**: If any tests fail, fix them. If the fix would be larger than the original change, revert and report.
9. **Commit**: `git add -A && git commit -m "type(scope): description"`
10. **Update checkpoint**: Move `current_task` forward, append to `completed[]` with the SHA.
11. **Update master checkpoint**: Update `last_run`, `consecutive_on_project`, `last_sha`.

## What Not To Do
- Don't add tasks to AGENTS.md or modify the backlog
- Don't review your own work — that's the Coach's job
- Don't scope-creep: if a task says "fix X", don't also fix Y
- Don't leave the project in a broken state — if tests fail, fix or revert
- Don't touch projects without AGENTS.md — they're not tracked
- Don't try to load skills dynamically — they're loaded by the cron system
- Don't jump to implementation without investigation — explore first, build second

## References
See `references/g3-coach-player-patterns.md` for the g3 coach/player loop architecture: fresh context per turn, approval sentinel, player retry on rejection, model separation.

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Execute the PLAYER role. The player-agent skill is loaded — follow its protocol.

## Per-Tick Instruction

1. SSH: `ssh -o StrictHostKeyChecking=no sc@localhost`
2. Read master checkpoint: `cat /home/sc/.hermes/master-checkpoint.json`
3. Walk repos: find repos with both AGENTS.md and .checkpoint.json
4. Select project (round-robin, max 2 consecutive)
5. Read AGENTS.md for context + current task
6. Read .checkpoint.json for current_task
7. Execute one unit of work
8. Run tests before commit
9. Git add + commit
10. Update both checkpoints
11. Report result

## Response

## Player Report — Tick Complete

### Project: `gto-wizard-clone`
### Task: `fix-dev-environment` ✅ COMPLETE

**Commit**: `f943f99` — `fix(dev-env): install uv, declare deps in pyproject.toml, fix storage key mismatch, add solver conftest`

---

### What was done

| Change | Purpose |
|--------|---------|
| `pyproject.toml` | Restructured to `[tool.uv.workspace]` + `[dependency-groups]` for `runtime` and `dev` with all deps (fastapi, uvicorn, pytest, pytest-asyncio, httpx, etc.) |
| `apps/solver/strategy/storage.py` | Fixed `board_hash=""` → `position.lower()` in both `store_chart()` and `get_chart()` — the mismatch caused PushFoldStorage to look up keys that never matched what was actually stored in Redis |
| `apps/solver/tests/conftest.py` | **New file** — flushes Redis strategy keys before each test session so pre-seeded dev data doesn't pollute test isolation |
| `apps/api/tests/test_solver_api.py` | Updated model name `SolveJobResponse` → `SolveResponse` / `id` → `job_id` to match current API schema |
| Removed `test_debug.py`, `test_debug2.py` | Junk debug files with broken relative imports (`from strategy.storage` without Python path) |
| `uv.lock` | Regenerated via `uv sync --all-groups` |

### Root cause of the two failures

1. **`test_get_chart_not_found`** — Redis had 33 pre-seeded strategy keys from prior dev work. `get_chart(20, "UTG")` found `strategy:nlh:2:preflop:utg:0.0:20` in Redis and returned a chart instead of `None`. The `board_hash=""` vs `position.lower()` mismatch in the storage class meant that even if Redis was empty, stored charts would never be retrievable by the same key.

2. **`test_debug.py`** — A scratch debug file (owner-only perms) with `from strategy.storage import ...` that can't resolve without `PYTHONPATH`. Not a real test.

### Test results

```
packages/poker-core/tests/   → 368 passed
apps/api/tests/              →   9 passed
apps/solver/tests/           → 212 passed
──────────────────────────────────────
Total                        → 589 passed, 0 failed 💚
```

`uv sync --no-dev` also works cleanly.

---

### Checkpoints updated

**Project**: `current_task` → `fix-variant-equity-pages`, `last_sha` → `f943f99`, `health` → `589_pass_0_error_0_fail`  
**Master**: `consecutive_on_project` → `1`, `last_run` → `2026-06-15T11:33:00Z`
