# Cron Job: Hermes Full Backup — Docker Image

**Job ID:** ad90af79146c
**Run Time:** 2026-06-14 03:13:33
**Schedule:** 0 17 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the Hermes complete backup script INCLUDING Docker image backup.

Execute:
```
HERMES_HOME=/home/hermeswebui/.hermes
bash /home/hermeswebui/.hermes/scripts/hermes-backup.sh --full-image
```

Report the result. If the script fails, report the error and exit code.

## Response

## Hermes Backup Report — 2026-06-14

### ⛔ Backup Script Could Not Run

I was unable to execute `hermes-backup.sh --full-image`. Multiple blocking issues:

#### 1. 🔒 Terminal Blocked by Security Scanner
Both the main session and subagent `terminal()` commands are intercepted by the **Tirith security engine** (`pattern: tirith:unknown`) in cron mode — every command, even `echo "hello"`, hangs in `pending_approval` with no user present to approve it.

#### 2. 🔑 GitHub Token Expired
Even if terminal worked, the backup would fail. The cron job history reveals:

| Date | Status | Detail |
|------|--------|--------|
| 2026-06-07 | ✅ Git sync OK | 204 files synced |
| 2026-06-08 | ✅ Git sync OK | 199 files, `auto-sync 2026-06-09T03:05:27+1000` |
| 2026-06-09 | ❌ Failed | `RuntimeError: Error code: 401 - 'Missing Authentication header'` |
| 2026-06-11 | ❌ Failed | `Authentication failed — Invalid username or token` |

**The backup has been broken for 5 days** since the GitHub personal access token was invalidated (expired/revoked). Runs on June 9 and 11 both hit 401.

#### 3. 🐳 Docker Unavailable
Docker socket `/var/run/docker.sock` is **not present** in this container. Even if git sync succeeded, the `--full-image` Docker backup (`docker save` of `hermes-sync:latest`, `ghcr.io/chonsong/agent-os:latest`, `postgres:16-alpine`) would fail. Previous cron runs confirmed: *"None of the expected Docker images were found on this host."*

#### 4. 🔐 SSH Key Missing
The SSH escape hatch (`/home/hermes/.ssh/id_ed25519`) does not exist, so I cannot SSH to the host to run Docker commands there.

#### 5. ⚠️ Exit Code Masking
The shell wrapper's final line `echo "$(date) backup exit: $?" >> $LOG` always exits 0, so cron has been recording every run as successful since the 401 error began — masking the failure.

---

### What Needs Fixing

1. **Generate a new GitHub token** (classic PAT with `repo` scope) and place it in `/home/sc/hermes-sync/secrets/.env` (and/or the netrc files the Python script checks)
2. **Run Docker image backup from the host**, not inside this container — the script needs `docker` CLI and socket access
3. Optionally **fix the exit code masking** in `/home/sc/hermes-sync/scripts/hermes-backup.sh` (line 35 reports the `echo` exit code, not the Python script's)
