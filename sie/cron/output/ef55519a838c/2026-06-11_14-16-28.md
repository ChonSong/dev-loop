# Cron Job: GTO Wizard — rebuild frontend + deploy

**Job ID:** ef55519a838c
**Run Time:** 2026-06-11 14:16:28
**Schedule:** 30 */3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Rebuild and deploy the GTO Wizard frontend on the host machine. All commands via SSH:
ssh -i /home/hermeswebui/.hermes/container_key sean@172.19.0.1 "command"

Steps:
1. SSH to host, cd /home/sean/gto-wizard-clone
2. git pull origin main
3. cd apps/web && /home/sean/hermes-apps-venv/bin/npm install (if package.json changed) && /home/sean/hermes-apps-venv/bin/npm run build
4. If build succeeds: pkill -f 'next-server' 2>/dev/null; sleep 2; nohup /home/sean/hermes-apps-venv/bin/npx next start -p 8564 > /tmp/gto-frontend.log 2>&1 &
5. Verify: curl -s -o /dev/null -w "%{http_code}" http://localhost:8564/
6. Also restart the API if needed: cd /home/sean/gto-wizard-clone/apps/api && PYTHONPATH='..' nohup /home/sean/hermes-apps-venv/bin/uvicorn main:app --host 0.0.0.0 --port 8003 --log-level warning > /tmp/gto-api.log 2>&1 &

If build fails, report the errors and don't kill the running server.
Reply with build status, HTTP codes, and git commit hash.

## Response

Deploy complete. Here's the report:

## GTO Wizard Frontend Deploy — Jun 11, 2026

**Status: ✅ Build & Deploy Successful**

### Git
- **Commit:** `9b291cd` — feat(study): wire study page to solver API, add preflop-range endpoint
- **New commits pulled:** 8 (from previous `1d26359`)

### Build Output
```
✓ Compiled successfully in 44s
✓ Generating static pages (23/23) — all routes
✓ Finalizing page optimization
```

23 routes built (analyze, bomb-pot, courses, double-board, equity, icm, omaha, play, plo, practice, spots, strategies, strategy, study, train, etc.)

### Deploy
| Service | Port | HTTP Status |
|---------|------|-------------|
| Frontend (Next.js) | 8564 | **200 OK** |
| API (FastAPI/uvicorn) | 8003 | **200 OK** (already running, not restarted) |

### New Features in This Deploy
- Dark header redesign + play/practice/study pages
- GTO Wizard dashboard with dual range grids, stats panels, action breakdown
- Study page wired to solver API with preflop-range endpoint
- Trainer API router for GTO training workflow
- MCCFR engine deployed with direct API integration
- Backend SQLite + fakeredis fallback for quiz/courses/spots endpoints
