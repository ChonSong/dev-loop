# Cron Job: Phase N: Lighthouse audit (FAILED)

**Job ID:** 9faeec8c1779
**Run Time:** 2026-05-12 21:06:06
**Schedule:** once at 2026-05-12 04:30

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Phase N: Lighthouse audit for hermes-web-computer.

WORKDIR: /opt/data/hermes-web-computer

CONTEXT: Phases K-M should already be committed. Check `git log --oneline -5` to confirm.

TASK:
Run Lighthouse performance, accessibility, and best practices audit on the running app.

IMPLEMENTATION:
1. Verify server is running: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3005/` — must return 200
2. If server not running:
   - `cd /opt/data/hermes-web-computer/frontend && npm run build`
   - `cd /opt/data/hermes-web-computer/backend && go build -o /tmp/hwc-server ./cmd/server/`
   - `PORT=3005 /tmp/hwc-server &` (background)
   - Wait 3 seconds, verify with curl
3. Install lighthouse if needed: `npm install -g lighthouse`
4. Run Lighthouse audit:
   ```bash
   lighthouse http://localhost:3005 --output json --output-path /opt/data/reports/lighthouse-results.json --chrome-flags="--headless --no-sandbox"
   ```
5. Parse results and extract key metrics:
   - Performance score
   - Accessibility score
   - Best Practices score
   - SEO score
   - Key metrics: FCP, LCP, CLS, TBT
6. Write results to `/opt/data/reports/lighthouse-audit.md` with:
   - Scores table
   - Key findings
   - Recommendations for improvement
7. Commit the report: `cd /opt/data/hermes-web-computer && git add /opt/data/reports/lighthouse-audit.md && git commit -m "docs: Lighthouse audit results" && git push`

REPORT: Lighthouse scores, key metrics, report location, any critical issues found.

## Error

```
RuntimeError: Connection error.
```
