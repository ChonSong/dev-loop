# Cron Job: GTO Wizard — rebuild frontend + deploy

**Job ID:** ef55519a838c
**Run Time:** 2026-06-11 15:33:46
**Schedule:** 30 */3 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Rebuild and deploy GTO Wizard frontend on the HOST, then restart all services. All commands via SSH:
ssh -i /home/hermeswebui/.hermes/container_key sean@172.19.0.1 "command"

CRITICAL CONTEXT from QA (2026-06-11 runs):
- wiz.codeovertcp.com returns 502 on MOST pages (/study, /equity, /icm, /train, /courses, etc.)
- But /play, /practice, /plo, /double-board, /bomb-pot return 200
- The study page was rewritten to call /api/v1/solver/preflop-range but frontend hasn't been rebuilt
- Port 8564 is holding the old build
- API runs on port 8003 and IS healthy (solver endpoints work)
- gRPC solver server on 50051 may have died

TASKS (in order, stop on first failure):

1. PULL LATEST CODE:
   cd /home/sean/gto-wizard-clone && git fetch origin && git status --short
   If local changes: git stash
   git pull origin main

2. BUILD FRONTEND (from apps/web/):
   cd /home/sean/gto-wizard-clone/apps/web
   export PATH="$HOME/.hermes/node/bin:$PATH"
   export NEXT_PUBLIC_API_URL=http://localhost:8003
   
   # Clean stale build artifacts
   rm -rf .next
   
   # Install deps
   npm install 2>&1 | tail -5
   
   # Build
   npx next build 2>&1 | tail -20
   
   If build fails, collect errors: npx next build 2>&1 | grep -E "Error:|Type error:|Module not found" | head -20
   Try to fix with: npm install critters 2>/dev/null; then retry build

3. DEPLOY FRONTEND:
   # Kill old frontend (host may auto-restart — use pkill -9)
   pkill -9 -f 'next-server' 2>/dev/null || true
   sleep 3
   
   # Start new frontend
   nohup /home/sean/hermes-apps-venv/bin/npx next start -p 8564 > /tmp/gto-frontend.log 2>&1 &
   
   # Wait for startup
   sleep 5
   curl -s -o /dev/null -w "Frontend: HTTP %{http_code}\n" http://localhost:8564/
   
   # Check title
   curl -sL http://localhost:8564/ 2>/dev/null | grep -o '<title>[^<]*</title>'
   # Expected: <title>GTO Wizard</title>

4. RESTART API (solver router was rewritten):
   pkill -f 'uvicorn.*8003' 2>/dev/null || true
   sleep 2
   cd /home/sean/gto-wizard-clone/apps/api
   PYTHONPATH='..' nohup /home/sean/hermes-apps-venv/bin/uvicorn main:app --host 0.0.0.0 --port 8003 --log-level warning > /tmp/gto-api.log 2>&1 &
   sleep 4
   curl -s -o /dev/null -w "API: HTTP %{http_code}\n" http://localhost:8003/api/v1/health
   curl -s http://localhost:8003/api/v1/solver/health | head -c 100

5. RESTART gRPC SOLVER (may have died):
   pkill -f 'solver.server' 2>/dev/null || true
   sleep 1
   cd /home/sean/gto-wizard-clone
   nohup /home/sean/hermes-apps-venv/bin/python3 -m apps.solver.server > /tmp/solver-grpc.log 2>&1 &
   sleep 3
   ss -tlnp | grep 50051 || echo "gRPC not started (ok, not needed for direct solve path)"

6. VERIFY SOLVER:
   curl -s -X POST http://localhost:8003/api/v1/solver/solve -H 'Content-Type: application/json' -d '{"board":"Kd7h2c","iterations":100,"street":"river"}' | head -c 200
   curl -s -X POST http://localhost:8003/api/v1/solver/preflop-range -H 'Content-Type: application/json' -d '{"position":"BTN"}' | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Preflop: {len(d.get(\"hands\",[]))} hands')" 2>/dev/null || echo "preflop-range check failed"

7. LIVE SITE CHECK:
   # The live site at wiz.codeovertcp.com routes through the host's tunnel on port 8564
   # So if localhost:8564 works, the live site should work
   echo "Frontend port 8564: $(curl -s -o /dev/null -w '%{http_code}' http://localhost:8564/)"
   echo "API port 8003: $(curl -s -o /dev/null -w '%{http_code}' http://localhost:8003/api/v1/health)"

8. If everything works, push any build fixes:
   cd /home/sean/gto-wizard-clone
   git add -A && git diff --cached --stat | head -20
   # Only commit if there are meaningful changes (not .next/ build artifacts)
   git commit -m "fix(deploy): rebuild frontend, restart services" && git push origin main 2>/dev/null || echo "No changes to commit"

Reply with: build success/fail, frontend HTTP status + title, API health, solver health, preflop-range count, git hash.

## Response

All tasks complete. Here's the final deployment report:

---

## 🚀 GTO Wizard Deploy Report — 2026-06-12 01:30 UTC

| Check | Status | Detail |
|---|---|---|
| **Git pull** | ✅ | Already up-to-date at `9cb924e`, pushed sw.js update → `b0cab0b` |
| **Frontend build** | ✅ | All 20 routes compiled, including `/study` (3.48 kB) |
| **Frontend (port 8564)** | ✅ | **HTTP 200** — `<title>GTO Wizard</title>` |
| **API (port 8003)** | ✅ | **HTTP 200** — `{"status":"ok","engine":"MCCFR","detail":"Solver engine available"}` |
| **gRPC solver (50051)** | ✅ | Restarted, port 50051 listening (PID 565439) |
| **Solver solve** | ✅ | POST `/api/v1/solver/solve` → complete, strategy returned |
| **Preflop range** | ✅ | 169 hands returned for BTN |
| **Commit** | ✅ | `b0cab0b` — sw.js build hash update pushed |

**Summary:** All services rebuilt, restarted, and verified. Every endpoint is healthy. The 502 issue on `/study`, `/equity`, `/icm`, `/train`, `/courses` from yesterday's QA run should be resolved — the frontend now serves the rebuilt static content that calls the new `/api/v1/solver/preflop-range` endpoint correctly.
