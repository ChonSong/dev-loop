# Cron Job: Hermes Full Backup — Docker Image

**Job ID:** ad90af79146c
**Run Time:** 2026-06-11 17:05:25
**Schedule:** 0 17 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the Hermes complete backup script INCLUDING Docker image backup.

Execute:
```
HERMES_HOME=/home/hermeswebui/.hermes
bash /home/hermeswebui/.hermes/scripts/hermes-backup.sh --full-image
```

Report the result. If the script fails, report the error and exit code.

## Response

## Backup Result: FAILED

The backup run **failed** at the git authentication stage. Here's the breakdown:

### ❌ Git Sync — Failed (Step 1/3)

```
[WARN] fetch failed: remote: Invalid username or token
[ERROR] clone failed: Authentication failed for 'https://github.com/ChonSong/hermes-sync.git/'
```

**Root cause:** The GitHub personal access token (`ghp_...`) stored in `~/.netrc` has been invalidated — GitHub API returned **401 Unauthorized**. The token is expired or revoked.

You'll need to **generate a new GitHub classic token** (or fine-grained token with repo scope) and update it in:
- `/home/hermeswebui/.hermes/home/.netrc`
- `/home/hermeswebui/.hermes/hermes-sync/netrc` (if the sync repo still had a local copy)

The format is:
```
machine github.com
login ChonSong
password <new-token>
```

Or set the `GITHUB_TOKEN` environment variable (currently set but not used by the netrc lookup in the script — the script checks netrc files first and that stale token takes precedence).

### ❌ Docker Image Backup — Not Reached

The `--full-image` flag was passed, but the script never made it to the Docker stage because the git auth failure was fatal. Even if it had, **Docker is not installed inside the container**, so `docker save` would fail — the image backup step would need to run from the **host**, not from inside the container.

### ⚠️ Script Bug

The shell wrapper (`hermes-backup.sh`) always exits 0 regardless of the Python script's exit status because the final line `echo "$(date ...) backup exit: $?" >> "$LOG"` itself always succeeds. So cron treats every run as successful even when it actually failed.
