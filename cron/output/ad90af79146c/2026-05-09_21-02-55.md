# Cron Job: Hermes Full Backup — Docker Image

**Job ID:** ad90af79146c
**Run Time:** 2026-05-09 21:02:55
**Schedule:** 0 21 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the Hermes complete backup script INCLUDING Docker image backup.

Execute:
```
/usr/bin/python3 /opt/data/scripts/hermes-backup.sh --full-image >> /opt/data/logs/hermes-backup.log 2>&1
```

This backs up all hermes state AND saves Docker images (hermes-sync:latest, agent-os, postgres) to /opt/data/backups/docker-images.tar.gz. This takes several minutes and produces a ~5-6GB file that is then included in the next git sync.

Just run it — no reasoning needed.

## Response

Backup completed successfully. Here's the summary:

**✅ Backup Status: Done (exit code 0)**

| Step | Status |
|------|--------|
| Repo setup | ✅ |
| Data sync | ✅ 16 items synced |
| GitHub push | ✅ Pushed 3 files |
| Docker images | ⚠️ No images found |

**Details:**
- All state files backed up: config.yaml, SOUL.md, auth.json, kanban.db, state.db, skills, memories, workspace, sessions, plans, cron, scripts
- Pushed to GitHub: `auto-sync 2026-05-10T07:02:36+1000`
- Docker image backup: `hermes-sync:latest`, `agent-os`, and `postgres:16-alpine` were not found — Docker is not available inside this container (no Docker socket mounted)

**Note:** The Docker image portion (`--full-image`) only works when Docker is accessible. If Docker runs on the host, the image backup would need to be executed from the host machine or with the Docker socket mounted into the container.
