# Cron Job: hermes-web-computer: rebuild + deploy check

**Job ID:** ecb3846b907b
**Run Time:** 2026-05-23 17:14:27
**Schedule:** every 240m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Rebuild and verify hermes-web-computer:

1. cd /opt/data/hermes-web-computer && git pull
2. cd frontend && npm run build
3. cd backend && go build ./... && go test ./...
4. If both pass: restart the server (kill old, start new on port 3005)
5. curl localhost:3005 to verify

Report: build time, test results, server status. If anything fails, report the error.

Working dir: /opt/data/hermes-web-computer

## Response

**hermes-web-computer rebuild report**

- **git pull**: Already up to date
- **frontend build**: ✓ PASSED (55.54s, warnings only)
- **go build**: ✓ PASSED
- **go test**: ✗ FAILED — 6 tests in `backend/ws`

**Failed tests** (all in `hermes-web-computer/backend/ws`):
- `TestFSList_Root` — expected `fs.list.response`, got `fs.error` (path `/home/hermeswebui/.hermes/hermes-web-computer` not found)
- `TestFSList_Nested` — same path error
- `TestFSRead_TextFile` — same path error
- `TestFSStat_File` — expected exists=true
- `TestFSStat_Dir` — expected exists=true
- `TestWSFSRoundTrip` — connection closed before response (15s timeout)
- `TestWSFSWriteReadRoundTrip` — connection closed before response (15s timeout)
- `TestWSMultiProtocol` — fs.stat.response should report exists=true
- `TestWSReconnect` — reconnected session timeout (15s)
- `TestWSErrorHandling` — connection broken after malformed JSON

**Root cause**: Tests expect path `/home/hermeswebui/.hermes/hermes-web-computer` but it doesn't exist. Likely a missing test fixture setup or misconfigured test environment path.

**Server status**: NOT restarted — tests must pass before deploying.
