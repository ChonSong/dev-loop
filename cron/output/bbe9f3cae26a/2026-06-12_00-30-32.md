# Cron Job: GTO — frontend rebuild + strategy fix (FAILED)

**Job ID:** bbe9f3cae26a
**Run Time:** 2026-06-12 00:30:32
**Schedule:** 30 0,3,6 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Rebuild GTO Wizard frontend with fixes. All commands via SSH:
ssh -i /home/hermeswebui/.hermes/container_key sean@172.19.0.1 "command"

GAPS found 2026-06-11:
- Frontend nav links to /strategies (plural) but API serves from /api/v1/strategy (singular) — mismatch
- Strategy lookup endpoint returns empty (no precomputed strategies)
- Some frontend pages use mock data instead of real API calls
- After rebuild, site should show "GTO Wizard" title

TASKS (in order):

1. PULL LATEST:
cd /home/sean/gto-wizard-clone && git pull origin main 2>&1 | tail -3

2. CHECK STRATEGIES PAGE for mock data or wrong API path:
grep -n "fetch\|axios\|api/\|mock\|MOCK\|const data" /home/sean/gto-wizard-clone/apps/web/src/app/strategies/page.tsx 2>/dev/null | head -20
If the page uses /api/v1/strategies (plural), fix to /api/v1/strategy (singular) with sed.

3. BUILD FRONTEND:
cd /home/sean/gto-wizard-clone/apps/web
export PATH="$HOME/.hermes/node/bin:$PATH"
export NEXT_PUBLIC_API_URL=http://localhost:8003
rm -rf .next
npx next build 2>&1 | tail -10
If build fails: npx next build 2>&1 | grep -E "Error:|Type error:|Module not found" | head -10

4. DEPLOY FRONTEND:
pkill -9 -f 'next-server' 2>/dev/null; sleep 3
nohup /home/sean/hermes-apps-venv/bin/npx next start -p 8564 > /tmp/gto-frontend.log 2>&1 &
sleep 6

5. VERIFY ALL 14 PAGES:
echo "Title: $(curl -sL http://localhost:8564/ 2>/dev/null | grep -o '<title>[^<]*</title>')"
echo "Pages:"
for p in / /equity /icm /train /courses /spots /strategies /analyze /study /play /practice /plo /double-board /bomb-pot; do
  echo -n " $p → "; curl -s -o /dev/null -w "%{http_code}" http://localhost:8564$p; echo
done

6. COMMIT fixes:
cd /home/sean/gto-wizard-clone && git add -A && git diff --cached --stat | head -10
git commit -m "fix(frontend): rebuild + fix strategy API path" && git push origin main 2>/dev/null || echo "No changes"

Reply with: build status, title, all 14 page status codes, git hash if committed.

## Error

```
RuntimeError: Error code: 401 - {'error': {'message': 'Missing Authentication header', 'code': 401}}
```
