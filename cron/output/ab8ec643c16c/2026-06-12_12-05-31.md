# Cron Job: Cron Health Check

**Job ID:** ab8ec643c16c
**Run Time:** 2026-06-12 12:05:31
**Schedule:** 0 12 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check all cron jobs and report health.

## Execution
1. List all cron jobs using the cronjob tool
2. Report how many are OK, how many are in error
3. For any in error state, list the job name, last run time, and last error status
4. If any jobs have been in error for more than 2 consecutive runs, flag them as critical

Provide a concise summary.

## Response

## Cron Health Report — Fri 12 Jun 2026

### Hermes Cron Jobs (managed)

| Job | Status | Last Run | Error |
|-----|--------|----------|-------|
| `hermes-sync-backup` | ❌ **Error** (disabled) | 2026-06-11 07:54 UTC | `OPENCODE_GO_API_KEY` missing |

**Finding:** Only 1 job exists in Hermes cron. It errored on its only run and has been left **disabled** (`enabled: false`). Since it's disabled it won't retry and can't accumulate 2+ consecutive failures — **not flagged critical**.

**Root cause:** `config.yaml` sets provider `opencode-go` but no API key is set. The job is a backup sync script (`scripts/hermes-sync-backup.py`). Needs either the `OPENCODE_GO_API_KEY` env var set or the provider switched before re-enabling.

---

### Host Cron Jobs (sean@localhost)

| Job | Schedule | Status |
|-----|----------|--------|
| `docker system prune` | Sun 03:00 | ✅ Last ran OK, next Sun Jun 14 |
| `pg-backup.sh` | Daily 03:00 | ✅ Running successfully — latest backup: Jun 12 (584K) |

---

### Systemd Timers

7 timers listed (shadow, plocate, logrotate, man-db, tmpfiles-clean, fstrim, archlinux-keyring-wkd-sync). All appear healthy with no overdue runs.

---

### Summary

- **Hermes jobs:** 1 total → 0 OK, 1 error (disabled, not critical)
- **Host crontab:** 2 total → 2 OK, 0 errors
- **Systemd timers:** 7 → 7 OK
- **Critical items (2+ consecutive errors):** None

**Action item:** The `hermes-sync-backup` job needs an API key config fix before it can run. Either set `OPENCODE_GO_API_KEY` or reconfigure the model/provider in `config.yaml`. Currently disabled, so not actively failing.
