# Cron Job: Cron Health Check

**Job ID:** ab8ec643c16c
**Run Time:** 2026-06-16 12:10:47
**Schedule:** 0 12 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check all cron jobs and report health.

## Execution
1. List all cron jobs using the cronjob tool
2. Report how many are OK, how many are in error
3. For any in error state, list the job name, last run time, and last error status
4. If any jobs have been in error for more than 2 consecutive runs, flag them as critical

Provide a concise summary.

## Response

Here's the full cron health report.

---

## Cron Health Report — 2026-06-16 12:09 AEST

### Infrastructure

| Component | Status |
|-----------|--------|
| Gateway (PID 605) | ✅ Running |
| Scheduler | ✅ Active, 17 jobs |
| Paused jobs | None |

### Summary

| Category | Count |
|----------|-------|
| ✅ Healthy | **15** |
| ⚠️ Delivery warning (historical) | **1** |
| ❌ Critical failure | **1** |

---

### ✅ Healthy Jobs (15)

Memory Curation, Morning Briefing, Hermes Full Backup, seans-reporepo refresh, hermes-web-computer rebuild+deploy, skill-selector-prep, html-in-canvas-api-monitor, Commitment Auditor, Daily QA Audit (wiz), Cron Health Check, GTO Wizard QA Sweep, player-development-loop, HWC Health Audit, GTO Deploy Log Rotation, coach-development-loop

All ran successfully at their last scheduled time with no errors. Spot-checks of actual output content (Memory Curation, HWC rebuild, Commitment Auditor, html-in-canvas monitor, GTO QA Sweep) confirm functional success — not just `last_status: ok`.

---

### ⚠️ Historical Delivery Warning (Non-Critical)

**context-budget-audit** (`64280d3687bf`)
- Schedule: `0 9 1 * *` (monthly)
- Last run: 2026-06-01 (15 days ago)
- Status: job ran OK, but delivery failed with `"deliver=origin"`
- The current config shows `deliver: local` — this was a pre-existing config issue from a previous run. Next run on July 1 should deliver correctly. **No action needed.**

---

### ❌ CRITICAL — Polytopia Deploy Loop

**Polytopia deploy loop** (`752d51adb96d`) — 🔴 **10+ consecutive failures**

| Detail | Value |
|--------|-------|
| Schedule | Every 5 minutes (no-agent script) |
| Script | `/home/sc/.hermes/scripts/deploy-polytopia.sh` |
| Error | **Script timed out after 120s** |
| Since | At least 10:52 AEST today (~1 hour of failures) |

**Root cause analysis:**
The script runs `npm run build` (which does `tsc && vite build`) then `npx vitest run`. The `dist/` directory exists and was updated at 12:02, so the build does complete — but it apparently takes too long, causing the 120s timeout. The script then tries to start/verify a `serve` process on port 3001 (which shows as already in use by another process). The combined build + test + server operations exceed the 120s limit.

**Potential fixes:**
1. Increase the script timeout in the cron job config (currently 120s default for no-agent)
2. Check if `tsc` is running slowly (TypeScript compiler with many errors?)
3. Check if `npx vitest run` is hanging on a test
4. Run build and test as separate, shorter cron jobs instead of chaining them in one script

---

### One Note

The **Memory Curation** output from yesterday noted several repos are stalled (hermes-web-computer 5 days, hermes-webui 7 days, energy-aware-task-router 3 days, and 6 repos inactive >30 days). These aren't cron failures — just observational notes from the curator's development audit. Worth a look when you're free.
