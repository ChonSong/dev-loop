# Session 2026-05-27 — Roadmap Engine Run

**Session:** `sess-2026-05-26-8e6f10` (date says 2026-05-26 in ID, ran on 2026-05-27 AEDT)  
**Command:** `cd /tmp/hermes-sync && python3 scripts/roadmap_engine.py --phase all`  
**Duration:** <1 min (05:04–05:04 AEDT) — **0 minutes reported, timezone bug**

---

## What Happened

- **Phase 1 ✅** — Clone/pull repos (hermes-agent already present, everything-dashboard up to date, openclaw up to date, repo-transmute already exists), TODOs/FIXMEs scan, coverage gaps, GitHub issues (no token), self-improvement skipped (script deleted). 617 learnings generated (massively inflated by duplication bug).

- **Phase 2 ⚠️** — Selected 2 tasks: `task-001` (translator.py tests) and `task-004` (Vitest for everything-dashboard). Both blocked immediately with `"No test framework detected"`.

- **Phase 3 ✅** — Report generated at `/tmp/hermes-sync/workspace/plans/nightly-reports/report-2026-05-26.md`.

---

## Key Bugs Confirmed Still Active

### Bug A: `_execute_test()` reports but doesn't scaffold
Both active tasks failed at the same hurdle. The executor correctly identifies that no test framework exists — but then stops instead of creating one. This is the blocking issue for `goal-001` (repo-transmute production-ready).

**Reproduction:**
```
[Phase 2] Selected 2 task(s) for tonight:
  - [task-004] Set up test framework for everything-dashboard (test, pri=3, effort=1.5h)
  - [task-001] Add test suite for translator.py core logic (test, pri=1, effort=2.0h)

[task-001] No test framework detected
[task-004] No test framework detected
```

**Fix needed:** When 0 tests collected, spawn coder subagent to scaffold `tests/` + config + smoke test, then re-run.

---

### Bug B: 617 learnings — massive deduplication failure
hermes-agent scanned as `proj-003` AND as the engine's own codebase simultaneously, producing ~200 copies of each TODO/FIXME entry. The same `roadmap_engine.py:138` line appears 3× in the learnings list alone.

**Root cause:** Session-wide deduplication not maintained across Phase 1 scan passes. Each pass re-adds the same findings.

---

### Bug C: Duplicate ideas from coverage-gap scanner
"Add test suite for openclaw monorepo" appears 4× identically. Coverage gap scanner not deduping by title before persisting.

---

### Bug D: Duration = 0 min
`Started: 05:04 AEDT · Ended: 05:04 AEDT` → Duration: 0.0h. `datetime.now()` timezone mismatch with UTC timestamps.

---

## Actual Script Inventory (2026-05-27)

```
/tmp/hermes-sync/scripts/
├── __pycache__/
├── reporters.py          ✅ 518 lines
├── roadmap.py           ✅ 429 lines
└── roadmap_engine.py    ✅ 1126 lines
```

`planner.py`, `executor.py`, `research.py`, `learnings_scanner.py`, `skill_author.py` are NOT present. The skill's "Scripts Location" table was wrong — all marked ✅ but only 4 of 10 files exist. The executor is fully inlined into `roadmap_engine.py`.

---

## Status of proj-005 (openclaw)

`proj-005` is defined as `ChonSong/agent-os` (correct repo) with `local_path: projects/openclaw`. The actual checkout at `/tmp/hermes-sync/projects/openclaw/` is the `agent-os` repo (cloned under the wrong directory name). All 9 `task-openclaw-*` tasks are `done` from prior sessions — no current blocking issues from this.

---

## Disk Space

86% used — elevated but not critical. Noted in Health Snapshot.
