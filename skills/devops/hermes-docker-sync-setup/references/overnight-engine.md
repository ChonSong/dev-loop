# Overnight Engine

Built in `hermes-sync/scripts/overnight_engine.py`. Replaces `gh` CLI with native `urllib.request` + `~/.netrc` PAT. Uses `/opt/hermes/.venv/bin/python` for pytest (system python3 has no pytest). See main SKILL.md for full details.